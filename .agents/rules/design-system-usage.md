---
trigger: always_on
---

- Always use the design system from sass folder;
- If you need some new css classes or new components, create a new component in sass;
- Always run composer run sass:build after change any .scss or .sass file;