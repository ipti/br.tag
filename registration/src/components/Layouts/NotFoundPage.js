import React from "react";
import NotFound from "../../screens/PageError/NotFound";

const NotFoundPage = props => {
  return (
    <>
      <NotFound />
    </>
  );
};

export default NotFoundPage;
