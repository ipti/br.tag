CREATE TABLE `inconsistency_sagres` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `enrollment` varchar(100) NOT NULL,
  `school` varchar(150) NOT NULL,
  `description` varchar(150) NOT NULL,
  `action` varchar(150) NOT NULL,
  PRIMARY KEY (`id`)
); 